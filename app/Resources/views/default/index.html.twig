{% extends 'base.html.twig' %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">FlyAround</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Dropdown
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('flight_index') }}">Flight</a>
                        <a class="dropdown-item" href="{{ path('planemodel_index') }}">Planemodel</a>
                        <a class="dropdown-item" href="{{ path('reservation_index') }}">Reservation</a>
                        <a class="dropdown-item" href="{{ path('review_index') }}">Review</a>
                        <a class="dropdown-item" href="{{ path('site_index') }}">Site</a>
                        <a class="dropdown-item" href="{{ path('user_index') }}">Users</a>
                    </div>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </nav>

    <div class="container">
            <h1>Routes disponibles :</h1>
            <ul>
                <li>
                    <a href="{{ path('planemodel_index') }}">Voir les avions</a>
                </li>
                <li>
                    <a href="{{ path('reservation_index') }}">Voir les reservations</a>
                </li>
                <li>
                    <a href="{{ path('flight_index') }}">Voir les vols</a>
                </li>
                <li>
                    <a href="{{ path('site_index') }}">Voir les terrains</a>
                </li>
                <li>
                    <a href="{{ path('review_index') }}">Voir les reviews</a>
                </li>
            </ul>
    </div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
